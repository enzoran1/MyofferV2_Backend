import { Injectable } from '@nestjs/common';

@Injectable()
export class SubscriptionService {
  getSubscriptionById(id: string) {
    // Implémentez la logique pour récupérer un abonnement par son ID ici
  }
}
