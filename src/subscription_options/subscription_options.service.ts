import { Injectable } from '@nestjs/common';

@Injectable()
export class SubscriptionOptionService {
  getSubscriptionOptionById(id: string) {
    // Implémentez la logique pour récupérer une option d'abonnement par son ID ici
  }
}
